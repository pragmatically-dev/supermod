# Build artifacts (regenerated by chiplinks-backend/build.sh)
chiplinks-backend/build/
chiplinks-backend/.qmake.stash
chiplinks-backend/Makefile
chiplinks-backend/*.so
chiplinks-backend/*.so.*
chiplinks-backend/*.zip

# OS / editor cruft
.DS_Store
Thumbs.db
